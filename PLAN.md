# מסמך תכנון מפורט — מערכת ניהול משחק טריוויה "פונקט פארקערט"

> **סטטוס:** טיוטה לאישור
> **תאריך:** 2026-07-25
> **גרסה:** 1.0

---

## 1. סיכום ההחלטות שהתקבלו

| נושא | החלטה |
|---|---|
| סדר פיתוח | קודם POC בריפו `trivia-dual-screen-poc`, אחר-כך המערכת המלאה בריפו `trivia-event-system` |
| פריסה פיזית | מחשב אחד עם שני מסכים מחוברים (חלון אדמין + חלון תצוגה) |
| שפה | עברית מלאה, RTL, בשני המסכים |
| הזנת תוכן | **ללא קבצי JSON כלל.** מסך עריכה מלא לשאלות ולשחקנים בתוך האדמין, כולל ייבוא מקובץ (רשימת שמות, רשימת שחקנים, תיקיית תמונות) ועריכה תוך כדי אירוע |
| סאונד | חלק מהתכנון מההתחלה (אפקטים: נכון/שגוי, טיימר, ניצחון) |
| הכרזת מנצח | ידנית תמיד, בשליטת המפעיל |
| קהל יעד | **מפעיל לא-טכני** — המוצר הסופי נארז כאפליקציית שולחן עבודה (אלקטרון): לחיצה כפולה פותחת אוטומטית את שני החלונות, ללא טרמינל וללא דפדפן |

> **הערה (26.07.2026):** מסמך האפיון המחייב והמעודכן הוא `docs/SPEC.html` — כולל שמות השלבים
> (הסיבוב המהיר / ראש בראש / פוקר פייס + חזיון תעתועים / הגמר הגדול) וכללי ההעפלה המעודכנים.
> בכל סתירה בין מסמך זה ל-SPEC — הקובע הוא SPEC.

### הוחלט (עדכון 26.07.2026)

- **ניקוד שלב ב':** לזוג בלבד, אין ניקוד אישי. **הזוג המנצח בכל מקצה עולה לשלב ג'** (3 זוגות = 6 שחקנים).
- **שיבוץ זוגות בשלב ב' (1.3):** זוג לא יכלול שני שחקנים שהיו יחד באותה קבוצה בשלב א'.
- **העפלה משלב א':** 3 המובילים מכל קבוצה (12 עולים) — לא דירוג כלל-משחקי.
- **הגמר (הובהר ב-1.3):** 2 פיינליסטים כברירת מחדל, **הגדרה במערכת ל-3 פיינליסטים**; סבב מתוזמן של 2:00 דקות לכל מתמודד.
- **מוזיקת רקע (1.3):** זמינה לאורך כל המשחק כולל בזמן השאלות, בשליטת המפעיל.

### שאלות שעדיין פתוחות

- **חלוקת הדו-קרבות בשלב ג':** איך משובצים 6 שחקני הזוגות המנצחים ל-3 דו-קרבות. בתכנון: המערכת מציעה, המפעיל עורך.
- **שקלול 2 הפיינליסטים:** הנחה נוכחית — ניקוד אישי משלבים א'+ג' + ניקוד חיצוני. רכיבי השקלול יהיו הגדרה גמישה.

---

## 2. ארכיטקטורה

### 2.1 עקרונות

- **אפליקציית ווב אחת** — React + Vite + TypeScript.
- **שני מסלולים (Routes):**
  - `/admin` — מסך המפעיל (שליטה מלאה, עריכת תוכן, ניקוד ידני).
  - `/display` — מסך הקהל (תצוגה בלבד, אפס כפתורים, מוצג במקרן ב-Fullscreen).
- **סנכרון:** `BroadcastChannel API` — תקשורת מיידית בין שני חלונות דפדפן על אותו מחשב. ללא שרת, ללא רשת, ללא נקודות כשל.
- **מקור אמת יחיד (Single Source of Truth):** חלון האדמין מחזיק את ה-State; חלון התצוגה הוא "ראי" — מקבל את ה-State המלא בכל שינוי ומרנדר אותו. חלון תצוגה שנפתח/מתרענן שולח בקשת `SYNC_REQUEST` ומקבל את המצב הנוכחי.

### 2.2 ניהול מצב (State) ו-Undo

- **מכונת מצבים (State Machine)** אחת מנהלת את כל מהלך המשחק. ה-State כולו הוא אובייקט JSON יחיד:

```
GameState = {
  version: מספר גרסת סכימה,
  screen: המסך הנוכחי (לוגו / שלב א-סבב / שלב ב-מקצה / ...),
  stage: 'A' | 'B' | 'C' | 'D',
  players: [...], teams: [...], pairs: [...],
  currentTurn: { playerId, questionIndex, ... },
  timer: { totalMs, remainingMs, running, ... },
  settings: { pairScoringMode, soundEnabled, ... }
}
```

- **Undo — Event Sourcing קל:** כל פעולת מפעיל (נכון/שגוי, ±1 ידני, מעבר מסך, התחלת טיימר...) נשמרת כרשומה במחסנית היסטוריה יחד עם snapshot של ה-State שקדם לה. "חזור" = שחזור ה-snapshot הקודם. ההיסטוריה נשמרת גם היא ב-Persistence, כך ש-Undo שורד רענון.

### 2.3 שרידות (Persistence)

- **IndexedDB** (ולא LocalStorage) — כי תמונות שלב ג' נשמרות כ-Blobs והן גדולות מדי ל-LocalStorage (מוגבל ל-5MB).
- נשמרים: מאגר התוכן (שחקנים, שאלות, תמונות), ה-GameState הנוכחי, ומחסנית ההיסטוריה.
- שמירה אוטומטית אחרי **כל** פעולה. רענון דפדפן / הפסקת חשמל / סגירת חלון → פתיחה מחדש חוזרת בדיוק לאותה נקודה, כולל זמן שנותר בטיימר (הטיימר נשמר כ"זמן שנותר" ולא כשעת יעד, וחוזר במצב מושהה — המפעיל לוחץ "המשך").
- כפתור "איפוס משחק" באדמין (עם אישור כפול) מוחק את מצב המשחק אך **לא** את מאגר התוכן.

### 2.4 טכנולוגיות

| רכיב | בחירה | נימוק |
|---|---|---|
| UI | React 18 + TypeScript | סטנדרט, מהיר לפיתוח |
| Build | Vite | פשוט, מהיר, אפס קונפיגורציה |
| State | Zustand + מנוע reducer ייעודי | קל-משקל, מתאים ל-snapshot/undo |
| סנכרון | BroadcastChannel | ללא שרת |
| אחסון | IndexedDB (עטוף ב-idb) | תמונות + state |
| עיצוב | CSS Modules / Tailwind + פונטים עבריים (Heebo/Rubik) | RTL מלא |
| אנימציות | CSS + canvas-confetti לגמר | קל ואמין |
| סאונד | Web Audio / `<audio>` עם קבצים מקומיים | ללא רשת |
| אריזה | **אלקטרון** + electron-builder | אפליקציה בלחיצה כפולה למפעיל לא-טכני |

**אריזת אלקטרון (הוכח ב-POC):** תהליך ראשי שמרים שרת סטטי פנימי על `127.0.0.1` בפורט **קבוע** ופותח שני חלונות — אדמין על המסך הראשי, קהל במסך מלא על המסך המשני (זיהוי אוטומטי). הפורט הקבוע הכרחי: IndexedDB נשמר לפי origin, ופורט משתנה היה מוחק את מצב המשחק בין הפעלות. סגירת חלון האדמין סוגרת את האפליקציה כולה.

**הערת הפצה:** בניית קובץ התקנה ל-Windows (exe) עם electron-builder נעשית בפועל על מכונת Windows (או CI). במהלך הפיתוח מריצים עם `npm run app`.

---

## 3. ניהול תוכן (מסכי עריכה באדמין)

**אין טעינת JSON.** כל התוכן מוזן ומנוהל דרך האדמין ונשמר ב-IndexedDB.

### 3.1 מסך ניהול שחקנים

- טבלת 20 השחקנים: שם, קבוצה (1–4), ניקוד נוכחי.
- הוספה / עריכה / מחיקה / העברה בין קבוצות (גרירה או בחירה).
- **ייבוא מקובץ:** הדבקת רשימת שמות (שורה לשם) או העלאת קובץ טקסט/CSV — עם מסך "שיוך לקבוצות" אחרי הייבוא.

### 3.2 מסך ניהול שאלות

- טאב לכל סוג: שלב א' / שלב ב' / שלב ג'-מיוחדות / שלב ג'-תמונות / שלב ד'.
- לכל שאלה: טקסט (+ תשובה אופציונלית שתוצג רק למפעיל).
- שאלות תמונה: העלאת תמונה (נשמרת ב-IndexedDB) + טקסט התשובה, וסידור לפי סדר.
- **ייבוא:** הדבקת רשימת שאלות (שורה לשאלה) / קובץ טקסט; לתמונות — בחירת תיקייה או ריבוי קבצים (`<input webkitdirectory multiple>`), מיון לפי שם קובץ.
- מונה מלאי מול צריכה צפויה (למשל שלב א': נדרשות 35×4=140 שאלות) עם התרעה על מחסור.

### 3.3 עריכה תוך כדי אירוע

- מסכי העריכה נגישים תמיד מהאדמין, גם באמצע משחק, בלי להשפיע על מסך הקהל.
- שאלה שכבר נשאלה מסומנת כ"נוצלה"; עריכתה משפיעה רק על הצגות עתידיות.
- הוספת/עריכת שחקן באמצע אירוע מותרת; מחיקת שחקן עם ניקוד — עם אזהרה.

---

## 4. מנגנונים גלובליים

1. **Undo מלא** — כפתור "חזור" קבוע באדמין; מחזיר שאלה אחת אחורה כולל ביטול הניקוד, או מסך אחד אחורה. (סעיף 2.2)
2. **פאנל ניקוד ידני אוניברסלי** — קבוע וגלוי בכל שלב באדמין:
   - שלבים א'/ג'/ד': רשימת השחקנים הפעילים עם ➕/➖ ליד כל שם (+גישה לכלל השחקנים).
   - שלב ב': עדכון גם לזוג כיישות וגם לשחקנים הבודדים.
   - כל לחיצה מסתנכרנת מיידית למסך הקהל עם אנימציה, בלי להפריע לזרימת השאלות, ונרשמת בהיסטוריית ה-Undo.
3. **מסך מעבר (לוגו)** — בין שלבים ובכל רגע שהמפעיל בוחר ("הצג לוגו"), מסך הקהל מציג את לוגו התוכנית עם אנימציה.
4. **בקרת טיימר** — בכל מסך עם שעון עצר: השהה/המשך + אפשרות איפוס.
   - **בזמן השהיה:** כפתורי "נכון"/"שגוי" ומעבר שאלות **נחסמים** (Disabled) למניעת טעויות; הכפתור הופך ל"המשך משחק"; מסך הקהל מציג את השעון קפוא עם חיווי "הפסקה" מהבהב עדין.
5. **סאונד** — אפקטים: תשובה נכונה, תשובה שגויה, 10 שניות אחרונות בטיימר, סיום זמן, חשיפת פיינליסטים, ניצחון. כפתור השתקה כללי באדמין. הקבצים מקומיים (bundled).

---

## 5. מהלך המשחק (Game Flow)

### שלב א' — הסיבוב המהיר
- 4 קבוצות × 5 שחקנים. סבב מחזורי קבוע: שחקן 1→5 וחוזר חלילה, **35 שאלות** לקבוצה (7 לשחקן).
- **העפלה: 3 המובילים מכל קבוצה** (12 עולים). שוויון על מקום 3 — התרעה ובחירה ידנית.
- אדמין: השאלה הנוכחית (+הבאה בתצוגה מקדימה), שם השחקן שבתורו, "נכון" (+1 ומעבר תור) / "שגוי" (מעבר תור), "שאלה קודמת", פאנל ניקוד ידני, מונה שאלות (12/35).
- קהל: השאלה בפונט גדול, טבלת 5 השחקנים עם השחקן הפעיל מודגש/מהבהב, ניקוד מתעדכן באנימציה.
- סיום 35 שאלות → כפתור "סיום קבוצה" → טבלת סיכום לקהל → מסך לוגו → טעינת הקבוצה הבאה (עד קבוצה 4).

### שלב ב' — ראש בראש
- **מעבר אוטומטי:** 12 העולים משלב א' (3 מכל קבוצה) מחולקים ל-6 זוגות. המערכת מציעה, המפעיל עורך ומאשר.
- 3 מקצים: זוג 1 מול זוג 2, זוג 3 מול 4, זוג 5 מול 6.
- מקצה: הצגת הזוגות לקהל → סבב זוג ראשון: טיימר 2:00, "התחל", שאלות זורמות, נכון=+1 לזוג / שגוי=שאלה הבאה, עד סיום הזמן (או עצירה ידנית) → סבב זוג שני זהה → מסך סיכום מקצה.
- **הניקוד לזוג בלבד. הזוג המנצח בכל מקצה עולה לשלב ג'.** שוויון במקצה — התרעה והכרעת מפעיל.

### שלב ג' — פוקר פייס + חזיון תעתועים (דו-קרב אישי)
- **משתתפים:** 6 שחקני שלושת הזוגות המנצחים משלב ב' → 3 דו-קרבות (המערכת מציעה חלוקה, המפעיל עורך).
- לכל דו-קרב שני חלקים:
  - **חלק א' — פוקר פייס (ללא זמן):** שאלות מיוחדות — 4 לכל שחקן, לסירוגין (8 סה"כ), נכון/שגוי מזכה את מי שענה.
  - **חלק ב' — חזיון תעתועים (טיימר גלובלי 2:00):** רצף תמונות — תמונה על מסך הקהל, חיווי בולט של מי בתור, נכון/שגוי → תמונה הבאה + התור מתחלף אוטומטית.

### שלב ד' — הגמר הגדול
1. **הזנת ניקוד חיצוני:** שדות קלט לכל אחד משחקני שלב ג' → "שקלל והצג פיינליסטים".
2. **חשיפת פיינליסטים:** המובילים בשקלול — **2 או 3 לפי הגדרה** — נחשפים על מסך הקהל באנימציה. שוויון → התרעה ובחירה ידנית.
3. **סבבי הגמר:** סבב מתוזמן של **2:00 דקות לכל מתמודד**, במתכונת מקצה ראש בראש.
4. **הכרזת מנצח:** לחיצה ידנית על "הכרז מנצח" → קונפטי + זיקוקים + מוזיקת ניצחון + שם המנצח במרכז מסך הקהל.

---

## 6. מקרי קצה

- **רענון / הפסקת חשמל:** שחזור מלא מ-IndexedDB, טיימר חוזר מושהה. (2.3)
- **שוויון בכל מעבר שלב:** התרעה + בחירה ידנית / נקודה מכרעת ידנית.
- **מחסור בשאלות באמצע סבב:** התרעה מוקדמת במסך העריכה + באנציה בזמן אמת; המפעיל יכול להוסיף שאלות תוך כדי.
- **מסך תצוגה נסגר בטעות:** פתיחה מחדש → `SYNC_REQUEST` → מסתנכרן מיידית.
- **לחיצה כפולה מהירה על "נכון":** debounce קצר + Undo זמין תמיד.

---

## 7. תוכנית פיתוח — שלבים ואבני דרך

> כל אבן דרך מסתיימת ב-commit + push + הדגמה, וממתינה לאישור לפני המשך.

### שלב 0 — POC (ריפו `trivia-dual-screen-poc`) ← **אנחנו כאן**
מטרה: הוכחת הטכנולוגיה בלבד. ראה `POC_PLAN.md` בריפו ה-POC.

### המערכת המלאה (ריפו `trivia-event-system`)
1. **תשתית:** שלד פרויקט, ראוטים, BroadcastChannel, IndexedDB, מנוע State+Undo (מבוסס לקחי ה-POC).
2. **ניהול תוכן:** מסכי שחקנים ושאלות + ייבוא (שמות, שאלות, תיקיית תמונות).
3. **שלב א' מלא** + פאנל ניקוד ידני + מסכי מעבר.
4. **שלב ב'** + מנגנון דירוג/חלוקה לזוגות + טיימר מלא.
5. **שלב ג'** (שאלות מיוחדות + רצף תמונות).
6. **שלב ד'** + מסך ניצחון.
7. **סאונד, אנימציות, עיצוב סופי** בהשראת הלוגו (סגנון גרפיטי צבעוני — ורוד/ירוק/צהוב זוהר על כהה).
8. **אריזה סופית:** אלקטרון + electron-builder — קובץ התקנה, אייקון, בדיקה על שני מסכים אמיתיים.
9. **חזרה גנרלית:** הרצת משחק מלא מדומה מקצה לקצה + תיקונים.
